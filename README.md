# ue5 multiplayer plugin
A C++ Unreal Engine multiplayer plugin that provides session creation, discovery, and player connection functionality. The plugin enables players to be matched and connected to multiplayer sessions using predefined rules and configurable parameters.
